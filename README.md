# llm-toolbox

Personal Claude Code tooling — shared across machines via Dropbox.

## Contents

- `CLAUDE.md` — core rules and instructions, loaded globally into every Claude session
- `INCEPTION.md` — the layered vault memory system and how `manage-sessions` + `dream` keep it up to date
- `skills/` — shared Claude Code skills (symlinked into `~/.claude/skills/` on each machine)

## Setup

See `~/base/src/dotfiles/AGENTS.md` → "Claude Code Setup".
